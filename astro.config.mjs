import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://clementloyer.github.io',
  // base: '/<repository-name>', // Only include this line if this is NOT a user/organization site (e.g., repo site)
});